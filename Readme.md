# ai-cli

## TODO

### aix

- [ ] esbuild bundle and publish `aix` for all node versions
- [ ] look into a way of persisting run commands into shell history?
- [ ] `--yolo` mode (runs in any level of danger)
- [ ] look into a way of reading thinking traces (maybe pressing ↑)
- [ ] interactive mode? `aix` with no arguments
