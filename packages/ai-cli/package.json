{
  "name": "ai-cli",
  "version": "0.5.3",
  "description": "The AI SDK for your terminal: generate text and media, and evaluate typed questions",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/vercel-labs/ai-cli.git"
  },
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "ai": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [
    "ai",
    "cli",
    "generative",
    "image",
    "video",
    "audio",
    "speech",
    "transcription",
    "text",
    "ai-sdk",
    "vercel"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "clean": "bun -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "bun run clean && bun build src/index.ts --target=node --outdir=dist",
    "typecheck": "tsc --noEmit",
    "format": "oxfmt --write src/",
    "format:check": "oxfmt --check src/",
    "lint": "oxlint src/",
    "test": "bun test",
    "prepack": "bun run typecheck && bun run build"
  },
  "dependencies": {
    "@ai-sdk/gateway": "4.0.85",
    "ai": "7.0.105",
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "typescript": "^6.0.3"
  }
}
